export interface SearchResult {
  id: string
  title: string
  description: string
  category: string
  url: string
  icon: string
}

export interface Language {
  code: string
  name: string
  flag: string
}